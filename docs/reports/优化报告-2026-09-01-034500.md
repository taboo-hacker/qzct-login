# qzct-login 全自动深度优化报告（2026-09-01）

> 由 project-auto-optimizer Skill v12.0 生成 · 2026-09-01 03:45
> 执行模式：自动执行（发现 → 仲裁 → 修复 → 独立验证 → 回归保护 → 提交全闭环，无人工干预）

## 1. 执行摘要

本轮为继 2026-08-31 之后的第二轮全自动深度优化。4 个只读 Reviewer 并行审查产出 33 条原始 Finding，主 Agent 逐条证据仲裁后归并为 32 条（0 CRITICAL / 3 HIGH / 18 MEDIUM / 11 LOW），两轮共 **修复 27 条**、新增 **87 个回归测试**（330 → 417 通过，覆盖率 79.96% → 83.26%），全程 black/isort/ruff/mypy 双模式 0 问题，两个独立 VerificationAgent 分别复核 Round 1（PASS，12/12 VERIFIED）与 Round 2（PASS_WITH_ISSUES，4/4 VERIFIED，其新发现的 1 个同族 MEDIUM 缺陷已由主 Agent 修复并钉板）。收敛依据：连续两轮复核无新的高优先级问题、可安全自动修复项全部完成，剩余 5 条低 ROI 重构与 3 项需用户决策事项（HTTP 明文 / 自签名证书 / DPAPI，均为上轮遗留）记入后续建议。

## 2. 项目概况

| 项 | 值 |
| --- | --- |
| 项目 | qzct-login v1.5.3（校园网自动登录 + 定时关机，Windows 桌面） |
| 技术栈 | Python ≥3.10 · PySide6 6.11 · requests · loguru · lunar-python/chinesecalendar |
| 结构 | core（领域）/ services（WiFi·登录·关机·任务链）/ infra（并发·日志·日期）/ gui（主窗口·对话框·组件·样式）/ utils · 约 6700 行生产代码 |
| 工具链 | pytest + pytest-qt + pytest-cov（门禁 70%）· ruff/black/isort · mypy（CI 双模式）· GitHub Actions CI/Release · PyInstaller |
| 特性约束 | 仅 Windows 生产环境（netsh/shutdown/icacls）；托盘常驻形态；无 AGENTS.md |

## 3. Runtime

约 105 分钟（估算，依据工作区文件时间戳）；2/3 轮（第 3 轮因收敛条件 B/A 达成而不再启动）。

## 4. Agent 统计

18 个子 Agent：4 Reviewer（并行发现）→ 主 Agent 仲裁 → 12 FixAgent（两波×两轮，文件组互斥并行）→ 2 VerificationAgent（独立复核）。详见 latest.json 的 agents 数组（含各 Agent 真实运行时长与 Finding 数）。确认率 100%（无 REJECTED——主 Agent 在仲裁阶段已剔除不可信项后才立任务）。

## 5. Baseline（2026-09-01 实测）

- Git：master @ 1d1ecbe，工作区干净（无用户未提交修改）
- 测试：330 passed / 1 skipped，覆盖率 79.96%
- ruff / black / isort：0 问题；mypy 双模式（venv 无存根 + --python-executable 真实 PySide6 存根）：66 文件 0 错误

## 6. Findings

| 严重度 | 数量 | 修复 | 遗留 |
| --- | --- | --- | --- |
| CRITICAL | 0 | — | — |
| HIGH | 3 | 3（B1 超时预算、B5+UX-01 链失败静默、UX-02 布防不可见） | 0 |
| MEDIUM | 18 | 13 | 5（CQ-01/CQ-05/CQ-07/CQ-08/UX-09，见 §13-14） |
| LOW | 11 | 11 | 0 |
| 合计（去重后） | 32 | 27 | 5（另 UX-03 部分修复：跨零点已修，"下次执行时间"属新功能） |

去重示例：BugHunter B5 与 UXReviewer UX-01 为同源问题（`_on_chain_error` 无托盘通知），合并为一条。

## 7. 已修复问题（按 7 个提交分组）

| 提交 | 内容 |
| --- | --- |
| `89b7557` fix(core) | 畸形 DATE_RULES 只重置该字段（原整份回退默认→保存后设置与凭据全部丢失）；区间/调休列表逐元素清洗（防启动崩溃循环）；save_config 临时文件 mkstemp 收权+失败清理；is_date_in_period 纵深防御；validate_period 单源 |
| `b811e50` fix(services) | WiFi 任务超时预算动态化（原 120s < 退避最坏 375s+，链被误杀跳过登录与保底关机）；TaskChain 链级 timeout 覆盖；单线程池限时任务 fail-fast 守卫（原双提交同池必假超时）；提交失败错误消息不再一律误报"线程池已关闭" |
| `7e335be` fix(gui) | 链异常托盘 Critical 通知；布防状态可见（剩余时间标签+托盘 tooltip+托盘"取消关机"菜单）；规则来源阶梯与 core 唯一阶梯对齐（原优先级相反致同屏矛盾）+SOURCE_TEXT 单源；取消关机防重入先于确认框并可见反馈；跨零点刷新；通知图标分级；footer tooltip；WiFi 动态预算接线 |
| `bb96554` fix(gui) | 暗色彩色文字对比度达 AA（primary_fg：暗 7.47/6.50/5.37:1，亮 ≥5.10:1）；QPushButton/QTabBar 键盘焦点指示；日历执行日加粗（色盲可辨，主题刷新保持） |
| `74c6bf8` fix(gui) | DateRuleWidget/BaseHolidayWidget 草稿深拷贝隔离（原未保存编辑直接改写生效配置并经主题切换被静默持久化）；保存前"静默失效"配置检查（空执行日/自定义区间重叠/调休重复，确认框默认否，默认数据零打扰已钉板）；主题落盘失败反馈；区间校验单源 |
| `a3b49a5` security(ci) | 20 处 Actions 钉 commit SHA + 低权限 job 显式 contents:read；requests 下限 2.32.4（排除 3 个 CVE）；README 补 WLAN 明文副本说明与清除方法 |
| `b1018af` fix(utils) | umask(0o077) 覆盖 loguru 轮转新文件；crash.log 写入后收权；单实例管道消息校验（b"show" 才唤起，堵焦点抢夺）；修复客户端写缓冲从不送达的隐性缺陷 |

## 8. 测试结果

| 时点 | Passed | Failed | Skipped | 覆盖率 |
| --- | --- | --- | --- | --- |
| Baseline | 330 | 0 | 1 | 79.96% |
| 最终（全量复跑） | **417** | **0** | **1** | **83.26%** |

新增 87 个测试覆盖：配置畸形恢复/元素清洗/临时文件清理、超时预算公式与覆盖语义、单线程池守卫、链失败托盘通知、布防显示、忙标志解耦、确认框默认按钮、草稿隔离、保存前校验、对比度（WCAG 公式实测）、焦点规则、日历加粗、管道消息校验、crash.log 收权等。测试环境：系统 Python 3.14 + PySide6 6.11.2（.venv-test 无 PySide6，仅用于 lint）。

## 9. Verification

- **VerificationAgent-Round1：PASS**——12 项修复逐条 VERIFIED（独立重跑全量测试、双模式 mypy、offscreen 冒烟、独立脚本复验 F06/F07/F20/F01 语义）；新发现 2 个低级遗留（预算公式少计 1 次 netsh、守卫错误文案复用）已由主 Agent 修复
- **VerificationAgent-Round2：PASS_WITH_ISSUES**——TB1-TB4 全部 VERIFIED（独立重算 WCAG 对比度、离屏实证污染路径与默认数据零警告、QSS 特异性分析）；新发现 1 个同族既有 MEDIUM（BaseHolidayWidget.save_holidays 引用泄漏）已由主 Agent 修复并钉板，2 处失效测试 mock 已清理
- 主 Agent 终验：pytest 417 passed / 1 skipped；ruff/black/isort 0 问题；mypy 双模式 0 错误；`QT_QPA_PLATFORM=offscreen` 冒烟存活 12s 无 Traceback

## 10. 性能变化

NOT_EXECUTED——本轮无性能优化项执行。上轮结论维持：当前量级无用户可感知收益的预防性项（日志攒批、主题防抖等）仍列后续建议。

## 11. 安全改进

- 供应链：全部 GitHub Actions 钉 40 位 commit SHA（重点 softprops/action-gh-release 运行于 contents:write job），低权限 job 显式 read；requests 下限排除 CVE-2023-32681 / CVE-2024-35195 / CVE-2024-46081
- 凭据落盘：config.json 临时文件 POSIX 0600（mkstemp）+ 失败清理；loguru 轮转新文件经 umask(0o077) 收权；crash.log 写入后收权
- 本地骚扰面：单实例命名管道改为消息内容校验（任意进程连接不再强制置前窗口）
- 文档化已知暴露面：WiFi 密码在 Windows WLAN profile 的第二份明文副本（README 安全提示 + 清除方法）；管道名抢占风险（模块 docstring，DoS 级接受）

## 12. Regression

无回归：Baseline 测试全绿保持全绿（330→417 全通过，无失败/无 skip 变化）；既有断言仅 3 处按预期行为变化更新（规则来源文案优先级修复、重叠检查口径、mypy 存根修正），均有独立验证背书；冒烟正常。mypy 双模式、black/isort/ruff 全程未出现新增问题。

## 13. Blocked

无 BLOCKED 任务（14/14 完成，0 回滚）。

## 14. Requires Human（均为上轮遗留，本轮未产生新的）

| # | 事项 | 说明 |
| --- | --- | --- |
| 1 | 校园网认证 HTTP 明文 | 网关协议限制；若网关支持 https 需提供证书做固定 |
| 2 | 自签名证书随包分发 | 既定设计；是否改用正规 CA 证书需决策 |
| 3 | 凭据 DPAPI/keyring 加密 | 需引入新依赖；建议与"WLAN profile 明文副本 user=all 是否改 user=current"一并权衡 |

## 15. Git

7 个逻辑单元提交：`89b7557` → `b811e50` → `7e335be` → `bb96554` → `74c6bf8` → `a3b49a5` → `b1018af`（+本报告提交）。38 个文件，+2384 / −211 行（不含报告）。工作区收敛后 clean（.optimizer/ 已加入 .gitignore）。

## 16. 回滚方式

```bash
# 整轮回滚（7 个代码提交整体回退）
git revert --no-commit 1d1ecbe..HEAD && git commit -m "revert: 回滚 20260901 自动优化"

# 单元回滚示例（按 §7 表格选哈希）
git revert b811e50   # 仅回退 WiFi 超时预算
```

## 17. 最终状态

**OPTIMIZATION_COMPLETE**。收敛依据：(B) 连续两轮独立复核无新的高优先级问题 + (A) 无 CRITICAL/HIGH 遗留且可安全自动修复项全部完成。第 3 轮不再启动（剩余项均为低 ROI 重构或需用户决策，继续优化预期收益低于回归风险，即条件 C）。

### 后续建议（未自动处理，按 ROI 排序）

1. **低风险重构尾巴**（CQ-01 任务链结果汇总提取纯函数 / CQ-05 测试回调样板收敛 / CQ-07 config_validator 同构循环提取 / CQ-08 农历文本纯函数化）——机械收敛，回归风险低，本轮因预算收敛让位
2. **设置草稿脏标记与撤销**（UX-09）：标签页标题 `*`、退出时提示、删除 Ctrl+Z——涉及交互设计决策，建议下轮单独任务
3. **"下次执行时间"概念**（UX-03 剩余部分）：任务链仅启动时执行一次，属新功能（调度器）而非缺陷
4. **对比度残留小项**：badge work 状态、outline_danger 暗色、QLineEdit:focus 边框（ VerificationAgent 复核实测 2.0~4.0:1，非文字组件 3:1 指引部分不达标）
5. **Dependabot/SHA 更新流程**：钉 SHA 后 Dependabot 默认不更新，需配置 versioning hint
6. 上轮遗留性能项与新功能类（开机自启、恢复默认按钮）维持原建议
